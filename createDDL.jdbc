CREATE TABLE WIDGET (ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, DESCRIPTION VARCHAR(255), NAME VARCHAR(255), QUANTITY INTEGER, PRIMARY KEY (ID))
CREATE TABLE ORDER (ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, CUSTOMERNAME VARCHAR(255), DATE TIMESTAMP, PRIMARY KEY (ID))
CREATE TABLE ORDER_WIDGET (Order_ID BIGINT NOT NULL, widgets_ID BIGINT NOT NULL, PRIMARY KEY (Order_ID, widgets_ID))
ALTER TABLE ORDER_WIDGET ADD CONSTRAINT RDERWIDGETwdgetsID FOREIGN KEY (widgets_ID) REFERENCES WIDGET (ID)
ALTER TABLE ORDER_WIDGET ADD CONSTRAINT ORDERWIDGETOrderID FOREIGN KEY (Order_ID) REFERENCES ORDER (ID)
